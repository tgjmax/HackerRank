#include <stdio.h>
#define MAX_SIZE 50

int main() 
{
  char ch, wrd[MAX_SIZE], s[MAX_SIZE];
  scanf("%c", &ch);
  scanf("%s", wrd);
  getchar();
  scanf("%[^\n]s", s);

  printf("%c\n", ch);
  printf("%s\n", wrd);
  printf("%s\n", s);
  return 0;
}
