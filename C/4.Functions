#include <stdio.h>
int max_of_four(int a, int b, int c, int d){
    int i,j;
    return ((i=(a>b)?a:b)>(j=(c>d)?c:d))?i:j;
}

  int main() {
  int a, b, c, d;
  scanf("%d %d %d %d", &a, &b, &c, &d);
  int ans = max_of_four(a, b, c, d);
  printf("%d", ans);

  return 0;
}

